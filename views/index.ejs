<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog App</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-4xl font-bold mb-8">My Blogs</h1>

    <form action="/tasks/" method="post" class="flex items-center mb-4">
        <input type="text" name="image" id="image" placeholder="Enter image URL" class="px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
      <input type="text" name="task" id="task" placeholder="Add a new task" class="mr-4 px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
      <input type="text" name="name" id="name" placeholder="Enter your name" class="px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
      <button type="submit" class="inline-block px-4 py-2 text-white bg-blue-500 rounded-lg hover:bg-blue-700">Add Task</button>
      <a href="/" class="inline-block ml-4 px-4 py-2 text-blue-500 hover:text-blue-700">View All Tasks</a>
    </form>

    <ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <% todos.forEach(function(todo) { %>
      <li class="rounded-lg shadow-md overflow-hidden bg-white p-4">
        <img src="<%= todo.image %>" alt="Blog image" class="w-full mb-4">
        <h2 class="text-xl font-semibold text-gray-800 mb-2"><%= todo.task %></h2>
        <p class="text-gray-600 mb-4">Written by <%= todo.name %></p>
        <div class="flex justify-between">
          <form action="/tasks/<%= todo._id %>/update" method="post" class="inline-block">
            <button type="submit" class="px-4 py-2 text-gray-600 hover:text-blue-500">Update</button>
          </form>
          <form action="/tasks/<%= todo._id %>/complete" method="post" class="inline-block">
            <button type="submit" class="px-4 py-2 text-gray-600 hover:text-blue-500"><%= todo.is_completed ? 'Uncomplete' : 'Complete' %></button>
          </form>
          <form action="/tasks/<%= todo._id %>/delete" method="post" class="inline-block">
            <button type="submit" class="px-4 py-2 text-red-500 hover:text-red-700">Delete</button>
          </form>
        </div>
      </li>
      <% }); %>
    </ul>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>

</html>
